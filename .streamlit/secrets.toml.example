# Copy to `.streamlit/secrets.toml` (gitignored) and fill in.
# On Streamlit Cloud, paste the same contents into Settings -> Secrets instead.

RENTCAST_API_KEY = ""   # required — https://www.rentcast.io/
CLAUDE_API_KEY   = ""   # optional — https://console.anthropic.com/account/keys
